# Retired: Mask Rewriter

This experiment never produced approved POLYWORDS content and is not part of the runtime or
editorial pipeline. Do not run, revive, or build on it without Pete's explicit approval.

Never commit local keys, `.env`, workspaces, generated CSVs, or `dist`.
