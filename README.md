# POLYWORDS

Expo/React Native word-recognition game. The active development branch is
`play-screen-overhaul`.

```powershell
npm.cmd install
npm.cmd start
```

Start with `AGENTS.md`; current branch state lives in `CONTEXT.md`.
