# POLYWORDS Claude Workflow Redirect

Canonical workflow now lives in `docs/WORKFLOW.md`.

Use this file only as a redirect. Do not treat old patch numbers, old Boss position 12 notes, or old docs-sync rules as source of truth.

Current law:
- Active branch: `play-screen-overhaul`.
- Standard Hunts are 10 rounds.
- Round 10 is POLLY'S WORD / boss word.
- Returning Haunt slot is Round 8 / index 7.
- Visual work requires approved layout map before code.
- Patch history belongs in `CHANGELOG.md`.
- Current state belongs in `CONTEXT.md`.
- Full Hero Word-Book spec lives in `docs/HERO_WORD_BOOK_SYSTEM.md`.

Patch process:
- Read `docs/WORKFLOW.md`.
- One surgical patch at a time.
- Run typecheck after code patches.
- Do not commit without approval.
