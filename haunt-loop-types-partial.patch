diff --git a/app/game/types.ts b/app/game/types.ts
index d34e00b..f96d8ed 100644
--- a/app/game/types.ts
+++ b/app/game/types.ts
@@ -1,5 +1,7 @@
 export type SlangEra = 'CLASSIC' | 'RETRO' | 'OLD SCHOOL' | 'THROWBACK' | 'NOW' | 'FRESH';
 
+export type RoundMode = 'normal' | 'master' | 'hauntReturn';
+
 export interface GhostMeaning {
   wordId: string;
   word: string;
@@ -93,6 +95,7 @@ export type Mask = {
 export type WordStep = {
   kind: 'word';
   word: string;
+  roundMode?: RoundMode;
   emotionalRole: EmotionalRole;
   eventType: EventType | null;
   meanings: Meaning[];
@@ -115,6 +118,29 @@ export type WordStep = {
   postSessionPollyDuration?: number;
 };
 
+export type HauntWound = {
+  missedReal?: {
+    id: string;
+    text: string;
+    emoji?: string;
+  };
+  acceptedTrap?: {
+    id: string;
+    text: string;
+    emoji?: string;
+  };
+};
+
+export type HauntQueueItem = {
+  wordId: string;
+  word: string;
+  originalStep: WordStep;
+  wound?: HauntWound;
+  roundsUntilReturn: number;
+  hauntAttempts: number;
+  sourceStepIndex: number;
+};
+
 export type PhraseBreakStep = {
   kind: 'phraseBreak';
   phrase: string;
